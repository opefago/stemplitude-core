/**
 * Parent experience types — aligned with `web/src/lib/api/students.ts` and backend
 * `ParentChildActivityResponse` / `ParentWeeklyDigest`.
 */
export type {
  ParentActivityKind,
  ParentActivityItem,
  ParentWeeklyDigest,
  ParentChildActivity,
} from "../../lib/api/students";

export type { StudentProfile as ChildProfile } from "../../lib/api/students";

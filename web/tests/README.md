# Web Test Organization

- Put browser/runtime/unit tests for frontend code under `web/tests/`.
- Robotics parser/runtime tests live in `web/tests/robotics/runtime/`.
- Keep test files split by domain/language (for example: `pythonInterpreter.test.ts`, `cppInterpreter.test.ts`).

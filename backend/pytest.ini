[pytest]
asyncio_mode = auto
testpaths = tests
pythonpath = .
asyncio_default_fixture_loop_scope = function
markers =
    unit: Fast tests with mocked dependencies (no containers)
    integration: Tests using real PostgreSQL and Redis via testcontainers

# Backend Robotics Test Organization

- Put backend robotics API/service/schema tests under `backend/tests/robotics/`.
- Keep web runtime/parser tests in `web/tests/robotics/`.
- This split keeps frontend execution/runtime validation separate from backend contract/integration validation.
